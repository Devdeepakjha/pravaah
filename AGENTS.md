# Agent Guidelines & Workflow

## Continuous Git Synchronization
- All work performed in this repository must be tracked in Git.
- After completing any feature, fix, or milestone, ensure changes are verified, staged, committed with a descriptive message, and pushed directly to `origin main`.
- Never commit build artifacts (`.next/`), temporary caches, or `node_modules/`. Keep `.gitignore` strictly maintained.
